{
    "name": ".NET install",
    "id": "dotnet",
    "version": "0.0.2",
    "options": {
        "version": {
            "type": "string",
            "default": "9.0.102"
        }
    },
    "containerEnv": {
        "DOTNET_ROOT": "/usr/local/dotnet",
        "PATH": "/usr/local/dotnet:${PATH}"
    }
}
